<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.munifahsan.bioshedistributor">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bioshe_admin_distributor"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bioshe_admin_distributor"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiosheDistributor">
        <activity android:name=".ui.detailPermintaan.DetailPermintaanActivity"></activity>
        <activity android:name=".ui.mintaProduk.MintaProdukActivity" />
        <activity android:name=".ui.isiDataDiri.IsiDataDiriActivity" />
        <activity android:name=".ui.splashScreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>